name: interaction_prototype
description: Phase 0 prototype for braven_charts interaction architecture
version: 0.0.1
publish_to: 'none' # This is a prototype, not for publication

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: false
