# Build configuration for generating mocks
# Run: flutter packages pub run build_runner build

targets:
  $default:
    builders:
      mockito|mockBuilder:
        generate_for:
          - test/mocks/mock_definitions.dart
        options:
          # Generate mocks in the same directory as the source
          build_to: test/mocks/
          
  test:
    dependencies:
      - $default
    sources:
      - test/**
      - lib/**
    builders:
      mockito|mockBuilder:
        enabled: true