# Task 4 Verification Criteria
# ⚠️ HIDDEN FROM IMPLEMENTOR - For orchestrator/verifier only

task_id: 4
title: "Create Normalization Mode Enum"
phase: "foundation"

verification_checks:
  - id: "file_exists"
    description: "NormalizationMode enum exists in correct location"
    command: 'Test-Path "lib/src/models/normalization_mode.dart"'
    expected: "True"
    
  - id: "test_file_exists"
    description: "Test file exists"
    command: 'Test-Path "test/unit/multi_axis/normalization_mode_test.dart"'
    expected: "True"
    
  - id: "export_added"
    description: "NormalizationMode exported from models barrel file"
    command: 'Select-String -Path "lib/src/models/enums.dart" -Pattern "normalization_mode"'
    expected: "Contains export statement"
    
  - id: "enum_values"
    description: "All 3 required values present"
    command: 'Select-String -Path "lib/src/models/normalization_mode.dart" -Pattern "(disabled|auto|always)"'
    expected: "All 3 values found"
    
  - id: "documentation"
    description: "Doc comments on enum and values"
    command: 'Select-String -Path "lib/src/models/normalization_mode.dart" -Pattern "///"'
    expected: "Multiple doc comments"
    
  - id: "tests_pass"
    description: "All tests pass"
    command: "flutter test test/unit/multi_axis/normalization_mode_test.dart"
    expected: "All tests pass"
    
  - id: "static_analysis"
    description: "No static analysis issues"
    command: "flutter analyze lib/src/models/normalization_mode.dart"
    expected: "No issues found"

adversarial_checks:
  - id: "correct_values"
    description: "Values match spec exactly: disabled, auto, always"
    check: "No extra values, no different names"
    
  - id: "simple_enum"
    description: "Simple enum - no methods or extensions"
    check: "This is a data type, not behavior"

minimum_tests: 6

notes: |
  Task 4 is a simple enum similar to Task 1.
  Three modes control normalization behavior:
  - disabled: Never normalize
  - auto: Detect when needed (based on range differences)
  - always: Always normalize regardless of ranges
