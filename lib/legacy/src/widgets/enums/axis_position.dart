/// Axis positioning
///
/// Defines where axes are positioned relative to the chart.
enum AxisPosition {
  /// Bottom position (X-axis default)
  bottom,

  /// Top position
  top,

  /// Left position (Y-axis default)
  left,

  /// Right position
  right,
}
